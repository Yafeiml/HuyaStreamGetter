## 🚀 v1.3.2 更新说明 (Release Notes)

### 🌟 局域网真实主机 IP 自适应、一键配置与跨环境剪贴板复制增强

1. **🌐 局域网端点与 M3U 播放列表真实 IP 动态生成**
   - 彻底解决 Docker 容器内部虚拟网桥 IP（如 `172.20.0.2`）或访问 `localhost` 时外部设备不可连接的问题；
   - 支持在配置或看板中直接指定宿主机真实局域网 IP（`customHost`），M3U 订阅源和各频道流地址全自动转换为宿主机局域网地址（如 `http://192.168.10.2:9898/live/...`），Jellyfin、Apple TV、电视盒子和手机 100% 直连播放；
   - 控制面板【局域网中继端点】卡片支持点击 ✏️ 一键弹出设置，内置 WebRTC 本地网卡 IP 智能探测与预填。

2. **📋 全兼容剪贴板复制引擎**
   - 突破浏览器在非安全上下文（纯 HTTP 局域网 IP 访问）中禁用 `navigator.clipboard` 的限制；
   - 自动在现代 Clipboard API 与轻量 DOM 模拟复制之间无缝降级切换，局域网环境下点击一键复制 100% 成功；
   - 复制按钮增加绿色 **`✓ 已复制！`** 动效（持续 2 秒）与右下角 Toast 弹窗通知。

3. **🛠️ Docker Compose 一键升级脚本**
   - 新增 Windows 免配置升级脚本 **`upgrade.bat`**，支持双击一键拉取最新镜像、平滑替换重启并自动清理残留旧镜像。
