## 🚀 v1.3.2 更新说明 (Release Notes)

### 🌟 局域网主机自适应与跨环境剪贴板复制增强

1. **🌐 局域网端点与 M3U 播放列表动态自适应**
   - 彻底解决 Docker 容器内部虚拟网桥 IP（如 `172.20.0.2`）外部局域网无法访问的问题；
   - `/jellyfin.m3u` 播放列表及 `/api/status` 端点全面根据客户端请求的实际 Host（如 `http://192.168.1.100:9898`）动态生成，确保 Jellyfin / PotPlayer / 电视盒子 100% 直连可用；
   - 仪表盘【局域网中继端点】卡片自适应显示当前访问主机。

2. **📋 全兼容剪贴板复制引擎**
   - 突破浏览器在非安全上下文（纯 HTTP 局域网 IP 访问）中禁用 `navigator.clipboard` 的限制；
   - 自动在现代 Clipboard API 与轻量 DOM 模拟复制之间无缝降级切换，局域网环境下点击一键复制 100% 成功。

3. **✨ 交互反馈体验升级**
   - 复制 M3U 订阅源地址与 HLS 单流链接时，按钮即时切换为绿色 **`✓ 已复制！`** 动效（持续 2 秒）；
   - 右下角同步展示复制成功 Toast 浮动通知。
