## 🚀 v1.2.0 更新说明 (Release Notes)

### 🌟 新增特性 & 核心升级

1. **🔗 智能直播链接净化与全自动识别**
   - 支持粘贴携带各种追踪参数、活动后缀的长 URL（如包含 `?hotRank=...&session_id=...&launch_id=...`），系统自动过滤无效参数并净化为标准纯净直链；
   - 新增直播平台全自动识别，无需手动切换平台类型；
   - 支持秒级获取直播间真实标题与房间号，大幅提升频道录入体验。

2. **⚡ 平台 Cookie 权威真伪鉴权检测**
   - 接入三大平台官方接口进行实时真伪校验：
     - **哔哩哔哩 (B站)**：直连官方 `nav` 鉴权接口，校验登录有效性与大会员状态；
     - **斗鱼直播 (Douyu)**：调用 `userInfo` 接口校验 UID 与登录凭据；
     - **虎牙直播 (Huya)**：调用移动端及 UDB 鉴权通道校验凭据；
   - 彻底解决此前因模板占位符残留导致的“未输入却显示已授权”问题。

3. **⏰ 后台定时健康巡检**
   - 后台守护服务每隔 **30 分钟** 自动对已配置的平台 Cookie 进行静默巡检；
   - 服务启动或凭据保存时即时发起探测，状态秒级同步。

4. **🔴 频道卡片 Cookie 过期红字预警与极简状态**
   - 频道大盘卡片显示极简设计：
     - **有效授权**：`🟢 已授权`
     - **失效过期**：`🔴 已过期`（红色醒目高亮预警）
     - **未配置**：`⚪ 免登录`

5. **🎨 全新折叠式 Cookie 管理弹窗**
   - 重构“平台 Cookie 管理”弹窗，默认呈现清爽的 3 大平台状态概览卡片；
   - 支持针对单个平台的【✏️ 编辑】（按需展开输入框）、【🔍 检测】、【🗑️ 清空】操作，以及弹窗底部一键【🔍 检测全部平台有效性】。

6. **🛠️ 缺陷修复与稳定性提升**
   - 修复请求头非 ASCII 字符过滤问题，解决斗鱼/虎牙推流请求出现 `Request headers must contain only ASCII characters` 异常；
   - 修复斗鱼直播间数据解析时的数值与布尔类型转换异常；
   - 优化新增频道弹窗在未输入链接时的初始展示。
