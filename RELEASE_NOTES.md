## 🚀 v1.3.1 更新说明 (Release Notes)

### 🌟 Cookie 鉴权健壮性与防误判机制升级

1. **🛡️ 平台鉴权网络弹性与自动重试复判**
   - 引入 **3 轮自动重试与指数退避机制**（Multi-Retry Backoff），针对连接超时、DNS 解析慢、SSL 握手抖动及 5xx 网关异常自动多次复判；
   - 只有平台官方服务器明确返回“凭据未登录/失效”响应时，才认定为失效。

2. **🔒 严格隔离“网络异常”与“Cookie 真实失效”**
   - 引入 `IsNetworkError` 状态隔离机制：网络通信抖动或暂时不可达时，**绝不改变原本已授权的凭据状态**；
   - 卡片继续保持 **`🟢 已授权`**，防止因偶发网络波动导致误报“🔴 已过期”红字。

3. **⚡ 底层通信优化**
   - 升级至 `SocketsHttpHandler` 连接池与 `HTTP/1.1` 协议栈，彻底修复 Linux / Docker 容器内跨网络调用 B站/各大平台鉴权接口时的 SSL 连接抖动问题。
